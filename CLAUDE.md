# Momoc

## パッケージマネージャの利用
- pnpmを使用すること。
- npmによる管理は原則禁止。

## Git操作について
- ソース変更後、必ずコミットしてください。
- Pull作業については人間が行います。
- コミットの言語は日本語で行ってください。

## 実装前のPlanモード使用について
- コードの新規追加・変更を伴うタスクは、**必ずPlanモードで計画を立て、ユーザーの承認を得てから実装を開始**すること。
- Planモードでは以下を明示すること:
  - 参照した既存コードとその場所
  - 踏襲するパターン
  - 変更するファイルと変更内容
- ユーザーの承認なしに実装を開始してはならない。

## AskUserQuestion の使用について
- 実装方針が複数考えられ、どちらを選ぶかがユーザーの意図に依存する場合は、**Planモード内でAskUserQuestionを使って確認**すること。
- 以下の場合は確認不要（自己判断で進める）:
  - 既存パターンが明確に存在し、それに従うだけの場合
  - タスク内容から意図が明確に読み取れる場合
- 以下の場合は確認必須:
  - 既存パターンが複数あり、どれを踏襲するか判断できない場合
  - 要件が曖昧で実装方針が定まらない場合
  - 既存の設計を変更・破壊する可能性がある場合
- 些細な確認を繰り返してユーザーの手間を増やさないこと。確認は本当に必要な場合のみ行う。

## 実装前の一次ソース確認について
- 実装に必要な仕様・定義・値は、**必ず一次ソースを確認してから**実装すること。
- 一次ソースとは、プロジェクト内の既存コード、設定ファイル、依存ライブラリのソースコード、公式ドキュメントなど、実際の定義元を指す。
- 「知っているつもり」で値をハードコードしたり、推測でオリジナルの定義を作成してはならない。
- 自己知識やWeb検索の結果より、プロジェクト内の実ソースを常に優先すること。

## 実装前の既存パターン確認について
- 新しいUIや機能を実装する前に、**必ずプロジェクト内で同種・類似の実装をGrep/Globで検索**すること。
- 既存の実装パターン・コンポーネント・データ定義が見つかった場合は、それを再利用・踏襲すること。オリジナルの実装を新規に作らない。
- 確認対象の例:
  - UIコンポーネント（カラーピッカー、スライダー、セレクタなど）
  - データ定義（カラーパレット、定数、設定値など）
  - レイアウトパターン、スタイリング手法
- この確認を怠りオリジナル実装を作ることは禁止する。

## UIパネルの区別について
- **「TailwindCSSプロパティ」と言われたら `TailwindEditor.tsx` のみを対象とすること。`PropEditor.tsx` は開いてはならない。**
- 「プロパティ」「コンポーネントのプロパティ」と言われた場合は `PropEditor.tsx` を対象とすること。
- この区別を誤った場合は即座に差し戻し、正しいファイルで実装をやり直すこと。

## Skillについて
動作に合わせて関連するスキルを参照してください。

- **フロントエンドデザインに関するスキル**
  - [frontend-design](.agents\skills\frontend-design)
- **E2Eテストに関するスキル**
  - [e2e-testing-patterns](.agents\skills\e2e-testing-patterns)
- **コーディングに関するスキル**
  - [clean-code](.agents\skills\clean-code)
- **ダイアグラムに関するスキル**
  - [drawio-diagram](.agents\skills\drawio-diagram)